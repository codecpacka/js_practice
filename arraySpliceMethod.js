console.log("array splice method");
// note: array splice method is either used for below operation
// delete
// insert 

// at the same time

const arr=new Array(10).fill(7);
arr.splice(1,5);
console.log(arr);